# ダイコメ 手順1 ─ テスト版で1回だけ通す（司さん・30分）

> ★安全★ テスト版 `daikou-app-test` と 本番 `daikou-app` は**別のアドレス**。
> スマホの保存領域も完全に別なので、**ここで何をしても本番メーターの状態は1バイトも変わりません。**
> ★普段の端末・普段のアカウントでOK★（QRはまだ誰にも配っていないので汚す先が無い）

## 【1】パソコンで 会社を作る … 5分

ひらく: **https://daikou-app-test.vercel.app/dashboard.html**

1. メールアドレスを入れて「ログインリンクを送る」→ 届いたメールのリンクを踏む
   （普段の `zeroact24.729@outlook.com` でOK）
2. 「はじめに、会社を登録」が出たら
   - 会社名: `ZERO代行` / 連絡先: 空でよい / 使う台数: `4`
   - 「この内容で登録する」
3. ★**会社名が既に出ている場合は 2 を飛ばして【2】へ**★
   （8/1に「ZERO代行」を登録済みなので、こちらの可能性が高い。押すボタンは出ません）

**成功の印**: 会社名が大きく出て、上に5つのタブ（会社の設定 / 売上表 / 給料 / 月次集計 / 請求書↗）

## 【2】QRを出す … 1分

「会社の設定」タブ一番上の **「ドライバーに配るURL／QR」**
- 「QRを表示」→ QRが出る（スマホで作業するなら「URLをコピー」でも可）
- URLは `https://daikou-app-test.vercel.app/?c=…`（`?c=` が付いている）

## 【3】スマホで有効化する … 3分

スマホのカメラでQRを読む（またはコピーしたURLを開く）

**成功の印**: メーターが出て、パソコン側の「使用台数」が **0/4台 → 1/4台**（要・画面更新）
※メーターが出るのは正しい動きです（ドライバーに配る物なので）

## 【4】空車で1件だけ走る … 15分

★客は乗せない。回送か営業前で。★

1. 業務開始 → 2. 代行開始 → 3. 少し走る（1kmでよい） → 4. 精算 → 5. 業務終了

★**走っている間に、距離と料金がいつもどおりか一目見てください**★
（請求書の仕組みを載せた版です。距離・料金に触っていないことの実地確認）

## 【5】★一番大事★ メーターを開き直す … 1分

★**業務終了しただけでは、まだクラウドに上がりません。**★
実績を送るのは**アプリを開いた時**（と圏内に戻った時）です。

→ メーターを一度閉じて、もう一度開く（または画面を更新）

## 【6】成功かどうかを見る … 2分

パソコンで 事務所 →「売上表」タブ

- **成功**: 今日の日付の車の行が出て、件数1／実車距離／総走行距離／売上が入っている
- **まだ**: 「この月はまだ記録がありません」→【5】をもう一度（電波の良い所で）

## 失敗したとき

★何も壊れていません。消す物もありません。★

- **売上表に出ない** → 電波の良い所でメーターを開き直す（何度やっても二重にはなりません）
  それでも出なければ言ってください。倉庫を直接見て「端末まで来ていない」のか「送れていない」のかを切り分けます
- **スマホを元に戻したい** → 本番メーター `https://daikou-app.vercel.app/` を開けば今までどおり
  （別の保存箱なので業務データは触っていません）
- **会社を作り直したい** → 不要です。同じ会社URLを何度開いても台数は増えません

## 通ったら私がやること（本番同期）

1. 通す前後で本番倉庫の全表の行数を突き合わせ（`--diff`）
2. 一式を本番repo `Daikou-app` へ同期（1コミット）
   job-sync / dk-config / license-activate / customer-master / dashboard / uriage / kyuryo
3. ★`APP_BASE` を本番URLに★ ＋ 食い違いを赤にするガード（`tests/unit/dk-config-app-base-host.test.js`）
4. `git diff` で ★business.js / meter.js / 距離コアに差分ゼロ★ を確認
5. 未有効化の端末は何も送らない／送信失敗で業務が止まらない／距離と料金が不変 を確認
6. CI全通し
7. ★本番で司さん自身が1回通す★（会社→QR→自分のスマホ→1件→開き直す→売上表）
8. ★7が通ってから★ 従業員のスマホを1台ずつ有効化 → 各端末の30日ぶんが順に上がる
9. `DK_MEISAI_AUTOPUSH` は立てない
