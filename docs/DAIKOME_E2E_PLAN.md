# ダイコメ 端から端まで 1回通す — 計画（2026-08-02・追加4点を反映）

**司さんのOKが出たらこの通りに通します。まだ何もしていません。**

> 第1段階（①〜③）… 承認済み。★司さんの一言が出たら実行★（本番倉庫に書くため）
> 第2段階（④）　　… ★フラグを立てるのは別途、司さんの一言が出てから★
>
> **★2026-08-02 差し替え: 司さんの本アカウントではなく「テスト用アカウント」で通す★**
> （本アカウントで走ると、テスト行が司さんの集計と8月の締めに混ざり、消す作業が発生するため）
>
> **実行状況: ①〜③はまだ1つも実行していない。本番倉庫に入れた行はゼロ**（41表の控えで実測済み）。

---

## ★使うアカウントと端末（2026-08-02 差し替え）★

### アカウント: `exally.supoort+daikome-test@gmail.com`

- **Gmail は `+` 付きアドレスに公式対応**（`名前+なんとか@gmail.com` は同じ受信箱に届く）
  → **司さんは今の受信箱のまま**マジックリンクを受け取れる
- Supabase から見ると**別のユーザー**になる → `owner_id` / `user_id` が分かれる
- ★請求書アプリの表は全部 `user_id` を持っている★（`companies` / `meisai` / `issuer` / `payments` を実測）
  ＝**テスト用アカウントで入れた行は、司さんの画面に1件も出ない**。8月の締めにも混ざらない。

★`zeroact24.729+…@outlook.com` は使えない★ — **Outlook.com は `+` 付きアドレスに対応していない**
（`user+tag@outlook.com` は届かない）。当てにするところだったので実測で確認した。

### 端末: ★業務に使っていない予備の1台★（司さんに指定してもらう）

- 端末は**会社トークンを1つしか持てない**。テスト会社のURLで開くと**本番の会社から切り替わる**。
  戻せるが、**戻し忘れるとその晩の記録がテスト会社に入る**。
- よって **今夜の業務で使う端末は絶対に使わない**。予備が無いなら、この日は通さない。

### この差し替えで変わること

| | 本アカウント版（旧） | ★テスト用アカウント版（新）★ |
|---|---|---|
| 請求先「テスト建設」 | 司さんの24件に混ざる | **混ざらない**（別ユーザー） |
| `meisai` の行（第2段階） | 司さんの1,074件に混ざる | **混ざらない** |
| 8月の締め | ★消す作業が発生★ | **発生しない** |
| ダイコメの会社 | 既存の「ZERO代行」を使う | **テスト会社を新規登録**（1台契約） |
| 確認する画面 | 司さんの請求書アプリ | **テスト用アカウントで開いた同じアプリ** |

★注意★ テスト用アカウントは**自分の請求先マスタが空**から始まる。
第1段階①の「テスト建設（消してよい）」は、そのテスト用アカウントで登録する。

## ★本物の営業中にやらない（追加4）★

**客を乗せている代行では絶対にやらない。** 回送か、営業前の空車で通す。
理由は単純で、**万一止まった時に困るのが客だから**。数字は取り直せるが、客の信用は取り直せない。

あわせてその走行で、**請求書ボタンを押しても距離と料金が変わっていないこと**を実地で見る
（`distance_m` と料金に影響しないのが設計。★設計がそうなっている、では足りない。実地で確認する★）。

---

## ★先に言っておくこと（計画の穴を1つ見つけました）★

指示にあった④「請求書アプリの一覧にその行が出るか確認」は、**今のままでは起きません。**

`supabase/functions/dk-sync-jobs/index.ts:187`

```ts
if (Deno.env.get('DK_MEISAI_AUTOPUSH') !== '1') return;
```

自動投入は**既定オフ**で、指示でも「**立てるな**」となっています。つまり:

- ③まで（ダイコメ側に代行が記録される）は**今すぐ通せます**
- ④（請求書アプリの一覧に出る）は**フラグを立てない限り原理的に起きません**

なので**2段階に分けます**。①〜③を先に通し、④は司さんが「立てていい」と言った時に別途。
黙ってフラグを立てて通す、はしません（司さんが今そこへ手入力中で、二重になるため）。

---

## いま倉庫に入っている物（2026-08-02 実測）

| 棚 | 件数 | 意味 |
|---|---|---|
| `companies`（請求先マスタ） | **24** | 本物の取引先 |
| `meisai`（明細） | **1,099**（生き 1,074・最新 2026-07-31） | ★本物。昨日まで使っている★ |
| `dk_companies` | 10（うち司さんの「ZERO代行」は 8/1 登録済） | |
| `dk_shifts` / `dk_trips` | **0 / 0** | ★まだ一度も流れていない★ |

**＝今回入るのは「初めての1件」です。既存の1,074件には触りません。**

---

## 第1段階：①〜③（今すぐ通せる）

### 何を書き込むか（これで全部）

| # | 棚 | 増える数 | 中身 |
|---|---|---|---|
| 1 | `companies` | **+1** | 請求先「**テスト建設（消してよい）**」★テスト用アカウントの持ち物★ |
| 2 | `dk_companies` | **+1** | ダイコメの会社「テスト代行（消してよい）」（契約1台） |
| 3 | `dk_company_devices` | **+1** | 有効化する**予備の端末**1台 |
| 3 | `dk_shifts` | **+1** | 業務開始〜終了 1晩ぶん |
| 4 | `dk_trips` | **+1〜2** | 代行1件（請求書払い・上の会社を選ぶ） |
| 5 | `meisai` | **0（増えない）** | ★フラグがオフなので入らない。これが第2段階★ |

**消す物・書き換える物：無し。** 既存の行には1バイトも触りません。

### 手順

| | 誰が | やること | 開くURL |
|---|---|---|---|
| ⓪ | 司さん | **テスト用アカウントでログイン**（`exally.supoort+daikome-test@gmail.com`）※両方のアプリで同じアドレス | 両方 |
| ① | 司さん or 私 | 請求書アプリ →「設定」→ 会社マスタ → **「テスト建設（消してよい）」を1社追加** | `https://exally-test.vercel.app/daikou-seikyu.html` |
| ② | 私 | ダイコメの会社を**新規登録**（例「テスト代行（消してよい）」・**契約1台**） | `https://daikome-jimusho.vercel.app` |
| ③-a | 司さん | 事務所 →「会社の設定」→ URLをコピー → ★**予備の端末**で開く（業務用は使わない）★ | `https://daikou-app-test.vercel.app/?c=…` |
| ③-b | 司さん | スマホで **業務開始 → 代行開始 → 実車中に「請求書」→ テスト建設 を選ぶ → 精算 → 業務終了** | （スマホのメーター） |
| ③-c | 私 | 事務所の**売上表にその1件が出るか**を実機で確認 | `https://daikome-jimusho.vercel.app` |

★①と②が同じアカウントであることが要ります★（`owner_id` で繋がるため）。
**両方とも `exally.supoort+daikome-test@gmail.com` で入ってください**（司さんの本アカウントでは入らない）。

### ここで「通った」と言える条件（全部満たすまで言わない）

- `dk_shifts` に1件、`dk_trips` に1件入っている（実測）
- `dk_trips` のその行に **`payment_type='invoice'` と `customer_name='テスト建設（消してよい）'`** が入っている
- 事務所の**売上表の画面に**その日・その車の売上が出ている（★実UIで目視★）
- 司さんのスマホで**業務が最後まで止まらなかった**（★これが最優先。数字より業務★）
- **請求書ボタンを押しても距離と料金が変わっていない**（実地で確認・追加4）
- ★**増えたのが想定した表・想定した件数だけ**であること（下の「追加3」で機械判定）★

### ★追加3 — 前後で全部の表の行数を控える★

本番倉庫にSQLを当てた時、**前後で全表の行数が一致すること**を機械で確かめた実績がある。同じ型を使う。

```
実行前  node scripts/db-snapshot.mjs --save before
実行後  node scripts/db-snapshot.mjs --save after
        node scripts/db-snapshot.mjs --diff before after --expect e2e-stage1
```

想定（これ以外が1行でも動いていたら**そこで止めて報告**）:

| 表 | 想定 |
|---|---|
| `dk_shifts` | +1 |
| `dk_trips` | +1〜2 |
| `companies` | +1 |
| `dk_companies` | +1（テスト会社を新規登録するため） |
| `dk_company_devices` | +1 |
| `meisai` | **±0**（第1段階では絶対に増えない） |
| **他の全部**（`pay_*` / 他アプリの表 …） | **±0** |

---

## 第2段階：④（司さんが「立てていい」と言ってから）

### ★「1行増えた」で終わらせない（追加1・最重要）★

自動投入の本当の怖さは「入らない」ではなく **「二重に入る」**。
本物の請求書アプリで二重になったら、**客に二重請求が行く**。

**合格条件（①だけでは不十分。②③まで通って初めて「安全」と言える）**

| | やること | 合格 |
|---|---|---|
| ① | フラグを立てて1件流す | `meisai` が **+1** |
| ② | ★**同じ勤務をもう一度送る**★ | `meisai` が **+0**（増えない）＝ `extra.dk_ref` が効いている証拠 |
| ③ | ★請求書アプリで**その行の備考を書き換えて保存** → もう一度同期★ | **+0 のまま**／★事務所が書いた備考が**消えていない**★ |

★**②③が通らないうちは `DK_MEISAI_AUTOPUSH` を戻す**★（オフに戻して報告）。

### ★追加2 — 入った1行の「中身」を照合する★

行が増えただけでは足りない。**メーターの確定値とファイルの中身が一致しているか**を1項目ずつ見る。

| 見る所 | 照合相手 |
|---|---|
| `date`（日付） | 代行した日 |
| `company`（会社名） | テスト建設（消してよい） |
| **`amount`（金額）** | ★**メーターの確定値そのものか**★ |
| `destination`（行き先） | 実際の到着地 |
| `distance`（km・小数1桁） | メーターの `distance_m` ÷1000 |
| `extra.dk_from`（出発地） | 実際の出発地 |

★**金額が1円でもズレていたら、そこで止めて報告**★。「入った＝合っている」ではない。

### 手順

1. `DK_MEISAI_AUTOPUSH=1` を Edge Function の secret に立てる
2. スマホから**もう1件だけ**代行を流す → ①
3. 同じ勤務を再送 → ② ／ 備考を書き換えて再同期 → ③
4. 追加2の照合を1項目ずつ
5. **フラグを戻すかどうかは司さんが決める**（②③が落ちたら私の判断で戻す）

★手入力から自動へ切り替える時期は司さんが決めます。私からは提案しません。★

---

## 絶対に守ること

- **入れた行を勝手に消さない。** 残したまま「これを入れた」と報告し、消すかは司さんが決める
- **本物の取引先名を使わない**（「テスト建設（消してよい）」で作る）
- **`DK_MEISAI_AUTOPUSH` は立てない**（第2段階で司さんの一言があるまで）
- **本物の営業中にやらない**（回送か営業前の空車で）
- **業務が止まったら即中止**して報告（距離・課金より業務が優先）
- 実機で最後まで動くのを見るまで「繋がった」と言わない。テストが緑なのは「繋がった」ではない

## ★報告に必ず入れること★

- **入れたテスト行は消さずに残す**（勝手に消さない）
- ~~8月の締めに混ざるので、どの月・どの会社・いくらか報告~~
  → ★**不要になった**★。テスト用アカウントで入れるので、司さんの集計・請求書・8月の締めに
  **1件も出ない**（`companies`/`meisai`/`issuer`/`payments` はすべて `user_id` を持つ・実測）。
  **消す作業も要らない。**
- **途中で中止した場合も、入った行はそのまま残して**「どこまで進んで何が入ったか」を報告
- ★追加3の突き合わせ結果は今までどおり必須★
  （想定外の表が動いていないかは、**アカウントに関係なく**見る）
- **どの端末を使ったか**を報告（本番の随伴車と取り違えていないことを示すため）

---

## 私が事前に済ませたこと（2026-08-02・★全部「読むだけ」。1行も書いていない★）

- [x] **突き合わせの道具を作った** `scripts/db-snapshot.mjs`（追加3）
      テスト先行 → ★わざと壊して赤くなるのを確認★（他アプリの表が動く／再送で二重請求 の2つが赤に）
      → 直して緑（16件）
- [x] **本番倉庫で実際に動かした（読むだけ）**：41表を控え、2回続けて控えて **動いた表 無し** を確認
      控えた実測値: `meisai 1,099` / `companies 24` / `dk_shifts 0` / `dk_trips 0` /
      `dk_company_devices 18`
      ※ `dk_company_devices` は前回報告の 0 ではなく **18**（過去のテスト端末が残っている）。
        司さんの「ZERO代行」は 0/4台。18件は他のテスト会社の分。
- [x] **Edge Function の疎通確認**（端末と同じヘッダで・書き込まない）
      `dk-sync-jobs` 引数なし → **400 `{"ok":false,"reason":"missing"}`** ＝生きている
      `dk-customers` GET → **405 `method`** ＝POST専用（生きている）
      ★触った後にもう一度控えて、倉庫が1行も動いていないことを確認済み★
- [x] ★引っかかった点★ ヘッダ無しで叩くと **401**（Authorization が要る）。
      端末は `DKConfig.headers()` が anon キーを付けるので問題なし。**私の probe の付け忘れだった。**

### 実行当日の流れ（この順で）

```
1. node scripts/db-snapshot.mjs --save before      ← 読むだけ
2. ①〜③を通す（司さんの手番を含む）
3. node scripts/db-snapshot.mjs --save after       ← 読むだけ
4. node scripts/db-snapshot.mjs --diff before after --expect e2e_stage1
   → ★DIFF RESULT: OK が出るまで「通った」と言わない★
```

**司さんの一言が出たら、1から流します。**
